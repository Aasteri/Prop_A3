# Local MySQL (Laragon / dev)
DATABASE_URL="mysql://root@localhost:3306/prop_a3"

# API
API_PORT=4000
API_URL=http://localhost:4000

# Web (Next.js)
NEXT_PUBLIC_API_URL=http://localhost:4000

# JWT (generate a strong secret for staging/production)
JWT_SECRET=change-me-in-production

# App
NODE_ENV=development

# Outbound mail (optional in dev — see docs/EMAIL.md)
# SMTP_HOST=server360.web-hosting.com
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=info@propa3.com
# SMTP_PASS=
# SMTP_FROM="Propa3" <info@propa3.com>
